# Testing Reference

- Reproduce with the smallest realistic fixture.
- Prefer deterministic tests over timing-sensitive checks.
- Add regression tests near the changed behavior.
- Use broader suites only when shared infrastructure or cross-cutting behavior is touched.
- For generated artifacts, verify both the generator command and the tracked output.
